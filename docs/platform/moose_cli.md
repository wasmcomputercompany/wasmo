Moose, the Wasmo CLI
====================

Wasmo has a companion command line application called `moose`.


Build and Run Moose
-------------------

```bash
$ ./gradlew os:cli:installDist
$ ./os/cli/build/install/moose/bin/moose
```

