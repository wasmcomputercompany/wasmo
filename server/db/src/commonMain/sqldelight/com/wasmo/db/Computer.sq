insertComputer:
INSERT INTO Computer(
  created_at,
  slug
)
VALUES (
  :created_at,
  :slug
) RETURNING id;

selectComputerBySlug:
SELECT *
FROM
  Computer
WHERE
  slug = :slug
LIMIT 1;

selectComputerById:
SELECT *
FROM
  Computer
WHERE
  id = :id
LIMIT 1;

