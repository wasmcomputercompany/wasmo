import app.rounds.game.identifiers.ComputerId;
import kotlin.time.Instant;

CREATE TABLE Computer (
  id BIGINT AS ComputerId GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at TIMESTAMPTZ AS Instant NOT NULL,
  slug VARCHAR(255) NULL
);

CREATE UNIQUE INDEX ComputerSlug ON Computer (
  slug
);
