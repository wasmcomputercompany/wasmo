import com.wasmo.journal.db.PersonId;
import kotlin.time.Instant;

CREATE TABLE Person (
  id BIGINT  AS PersonId GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at VARCHAR(255) AS Instant NOT NULL,
  name VARCHAR(255) NOT NULL UNIQUE
);
