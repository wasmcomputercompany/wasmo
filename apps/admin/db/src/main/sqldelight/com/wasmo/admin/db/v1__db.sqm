import com.wasmo.admin.db.AppInstallId;
import kotlin.time.Instant;

CREATE TABLE AppInstall (
  id INTEGER AS AppInstallId PRIMARY KEY,
  created_at TEXT AS Instant NOT NULL,
  app_version INTEGER NOT NULL,
  display_name TEXT NOT NULL,
  canonical_url TEXT
);
